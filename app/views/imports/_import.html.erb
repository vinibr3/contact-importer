<tr>
  <td> <%= import.contacts_file.try(:filename) %> </td>
  <td> <%= import.error_logs.count.positive? ? link_to(import.status, import_error_logs_path(import)) : import.status %> </td>
  <td> <%= import.created_at.strftime('%Y %m %d %H:%M') %> </td>
</tr>
